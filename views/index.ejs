<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
    <link rel="stylesheet" href="/css/styles.css">

  <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
  <!-- <script src="js/script.js"></script> Link to your script.js file -->
  </head>
<body>
  <form action="/logout" method="post" class="ms-2 d-flex align-items-center position-relative"> 
    <!-- <button class="btn btn-bg-primary text-whit h-100" type="submit">Logout</button> -->
    <button type="submit" class="btn btn-danger position-absolute top-0 end-0">Logout</button>
</form>
  <div class="container">
    <div class="main">
        <!-- <img src="/img/6012764.jpg" alt="" class="w-50 h-50"> -->
      <h1 class = "mt-5">Weather Application</h1>
      <form action="/weather" method="post" class="mt-5 mb-5" id="form-input">
        <div class="input-group input-group-lg">
            <span class="input-group-text" id="inputGroup-sizing-lg">Enter the city</span>
            <input type="text" id="city-input" name="city" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-lg">
          </div>
        </form>
    </div>

    <% if (weather) { %>
      <h1>History</h1>
      <% for (let i = 0; i < weather.length; i++) { %> 
        <div id="weather-info" class="bg-info rounded">
          <div>
            <h2>Weather in <%= weather[i].city %></h2>
            <p>Temperature: <%= weather[i].temperature %>°C</p>
            <p>Description: <%= weather[i].description %></p>
            <p><img src="<%= weather[i].icon %>" alt="Weather Icon"></p>
          </div>
          <div>
            <h2>Map</h2>
            <div id="map<%=i%>>" style="height: 300px;" class="mb-5" data-lat="<%=weather[i].lat%>" data-lon="<%=weather[i].lon%>" data-len="<%=weather.length%>"></div>
          </div>
        </div>

      <!-- <div>
        <h2>Map</h2>
        <div id="map" style="height: 400px;" class="mb-5"></div>
      </div> -->
      <% } %>
  <% } else if (error) { %>
      <p>Error: <%= error %></p>
  <% } %>

   <!-- index.html -->



    <!-- <div id="map" style="height: 400px;" class="mb-5"></div>
    <a href="/forecast-page" class="btn btn-primary">Check the 7 day forecast</a> -->
  </div>
      <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC_2YSJ_KR2XolIlxk8Jl6MlLTk8ahvzWM&libraries=places"></script>

      
      <script>
        function initMap() {
            let maps = document.querySelectorAll('[id^="map"]');
            
            maps.forEach(function(mapElement) {
                let lat = mapElement.getAttribute('data-lat');
                let lng = mapElement.getAttribute('data-lon');
                console.log(lat, lng);
                let map = new google.maps.Map(mapElement, {
                    center: { lat: parseFloat(lat), lng: parseFloat(lng) },
                    zoom: 10
                });
            });
        }
      
        window.onload = function() {
            initMap();
        };
      </script>
      
  
      <script src="/js/script.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>